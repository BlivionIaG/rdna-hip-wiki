# W8A16 — silicon / HIP contract

Engine row: [engine/fork-delta.md](../engine/fork-delta.md). Live files: `qdq_8_rdna2.cuh`, `moe_w8a16_rdna2.cu`. **Incomplete.** No `q_gemm_w8a16_rdna2.cu` at `9a344444` — dense may be missing or folded elsewhere.

**Not W8A8.** Activations stay fp16. Inner loop is `fdot2`, not `sdot4`.

## Native ops

| Step | Op |
|---|---|
| Load W | `uint64` = 8×i8 |
| i8 → fp16 | `__int2half_rn` |
| scale / zero | `__hfma2` (fp16) |
| A · W | `V_DOT2_F32_F16` (`__builtin_amdgcn_fdot2`) |

Same DOT as W4A16. Wider load, no nibble pack.

## Status

MoE in-tree. Dense file absent at tip. Treat as incomplete. Occupancy still first (FA / skinny).
